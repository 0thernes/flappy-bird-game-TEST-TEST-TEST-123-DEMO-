version: 2
updates:
  # Keep the SHA-pinned GitHub Actions fresh automatically, so pinning for
  # supply-chain safety does not mean they silently go stale.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "ci"
